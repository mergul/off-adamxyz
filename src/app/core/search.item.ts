export interface SearchUser {
    id: string;
    username: string;
    email: string;
    roles: string;
}

export interface SearchNews {
    id: string;
    topic: string;
    summary: string;
    date: string;
}
